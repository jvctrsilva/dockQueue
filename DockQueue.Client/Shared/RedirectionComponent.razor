@inject NavigationManager Navigation

<p role="alert">Não existe nada nesse endereço.</p>

@code {
    protected override async Task OnInitializedAsync()
    {
        try
        {
            Navigation.NavigateTo("/login");
        }
        catch (Exception e)
        {
            //Console.WriteLine(e);
        }
    }
}